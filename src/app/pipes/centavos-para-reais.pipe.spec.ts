import { CentavosParaReaisPipe } from './centavos-para-reais.pipe';

describe('CentavosParaReaisPipe', () => {
  it('create an instance', () => {
    const pipe = new CentavosParaReaisPipe();
    expect(pipe).toBeTruthy();
  });
});
